[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npx prisma db push --schema=src/db/schema.prisma && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
